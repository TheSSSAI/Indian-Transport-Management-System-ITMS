.env.example
# Example environment variables for local development using Docker Compose.
# Copy this file to .env and fill in the values.

# PostgreSQL connection details
# These are used by the Odoo service to connect to the database.
HOST=db
PORT=5432
USER=odoo
PASSWORD=myodoo-password
DB_NAME=tms_prod

# Odoo master password for creating/managing databases
# WARNING: Set a strong, unique password for production environments.
ODOO_MASTER_PASSWORD=my-master-password

# Odoo server configuration
ODOO_HTTP_PORT=8069

# Email configuration (for development, use a local mail catcher like MailHog)
EMAIL_FROM=tms-noreply@example.com
SMTP_SERVER=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_SSL=

# AWS Credentials for S3 and Secrets Manager integration
# For production, these should be managed via IAM roles. For local dev, they can be set here.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=

# S3 Bucket for storing attachments (receipts, PODs, etc.)
AWS_S3_BUCKET_NAME=