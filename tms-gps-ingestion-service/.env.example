# Application Configuration
LOG_LEVEL=INFO

# Third-Party GPS Provider API
GPS_PROVIDER_API_URL=https://api.examplegpsprovider.com/v1/locations
GPS_PROVIDER_API_KEY="your_api_key_here" # In production, this will be the ARN for AWS Secrets Manager
POLLING_INTERVAL_SECONDS=30

# RabbitMQ (Amazon MQ) Configuration
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
RABBITMQ_EXCHANGE_NAME=tms.events
RABBITMQ_ROUTING_KEY=location.updated

# AWS Configuration (for local development, can use dummy values if not connecting to AWS)
# For production, these are typically injected by the EKS environment (e.g., IRSA)
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=