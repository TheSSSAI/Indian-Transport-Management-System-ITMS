# This Custom Resource Definition (CRD) is for the AWS Secrets Store CSI Driver.
# It defines the `SecretProviderClass` resource, which is essential for securely
# fetching secrets from AWS Secrets Manager and making them available to pods.
# This file is a prerequisite for the secret management strategy outlined in REQ-1-503.
#
# Source: https://github.com/aws/secrets-store-csi-driver-provider-aws
# Version: v1alpha1
# It is managed outside of the Helm charts to prevent Helm from accidentally
# deleting it (and all associated secrets) on a `helm uninstall`.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  name: secretproviderclasses.secrets-store.csi.x-k8s.io
spec:
  group: secrets-store.csi.x-k8s.io
  names:
    kind: SecretProviderClass
    listKind: SecretProviderClassList
    plural: secretproviderclasses
    singular: secretproviderclass
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SecretProviderClass is the Schema for the secretproviderclasses
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SecretProviderClassSpec defines the desired state of SecretProviderClass
            properties:
              provider:
                description: "Configuration for provider name. \n The name of the
                  provider responsible for reconciling this SecretProviderClass.
                  This is a required field."
                type: string
              parameters:
                description: "Configuration for specific provider. \n This is a required
                  field."
                type: object
                x-kubernetes-preserve-unknown-fields: true
              secretObjects:
                description: secretObjects defines the desired state of synced K8s
                  secret objects
                items:
                  description: SecretObject defines the desired state of a synced
                    K8s secret object
                  properties:
                    secretName:
                      description: secretName is the name of the K8s secret object
                      type: string
                    type:
                      description: type is the type of the K8s secret object
                      type: string
                    labels:
                      additionalProperties:
                        type: string
                      description: labels are labels of the K8s secret object
                      type: object
                    annotations:
                      additionalProperties:
                        type: string
                      description: annotations are annotations of the K8s secret object
                      type: object
                    data:
                      description: data defines the desired state of K8s secret object
                        data
                      items:
                        description: SecretObjectData defines the desired state of
                          a K8s secret object data
                        properties:
                          key:
                            description: key is the key of the K8s secret object data
                            type: string
                          objectName:
                            description: objectName is the object name in the provider
                              that will be used as the value
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
            type: object
          status:
            description: SecretProviderClassStatus defines the observed state of
              SecretProviderClass
            properties:
              byPod:
                items:
                  description: SecretProviderClassPodStatus defines the observed
                    state of a pod using SecretProviderClass
                  properties:
                    namespace:
                      type: string
                    name:
                      type: string
                    objects:
                      items:
                        properties:
                          id:
                            type: string
                          version:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}